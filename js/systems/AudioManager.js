// AudioManager.js — FASE 10 / conforme necessidade (a implementar)
// Vai tocar músicas ambiente, SFX (spray, passos, alerta) e volume de vídeos
// via project.gameplay/videos. Usa Web Audio API (THREE.AudioListener é uma
// opção já que o resto do projeto usa three.js) para poder posicionar sons
// 3D (ex.: alarme da câmera de segurança).
export class AudioManager {
  constructor() {
    this.tracks = new Map();
  }
  play(id) {
    // TODO
  }
  stop(id) {
    // TODO
  }
}
