// TimelineSystem.js — Timeline Editor opcional (a implementar).
// Organiza eventos (VIDEO, WAIT, SPAWN, OBJECT_STATE, MISSION, SOUND, CAMERA,
// DIALOGUE) em uma linha do tempo editável visualmente no configurador
// (TimelineEditor.js) e executável em Play Mode/Preview Story via EventSystem.js.
export class TimelineSystem {
  constructor(tracks = []) {
    this.tracks = tracks;
  }
  play() {
    // TODO
  }
}
