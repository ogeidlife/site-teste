// VideoEditor.js — sistema de narrativa (a implementar)
// Ao selecionar um vídeo na VideoLibrary: configurar TRIGGER (ex.: MISSION
// START), BEFORE (ex.: PAUSE GAME), AFTER (ex.: ENABLE OBJECTIVE) e as
// opções Fade In/Fade Out/Skippable/Pause Music/Lock Player/Hide HUD
// (item 18 do spec de narrativa).
export class VideoEditor {
  constructor({ container, projectManager }) {
    this.container = container;
    this.projectManager = projectManager;
  }
  render(videoId) {
    // TODO
  }
}
