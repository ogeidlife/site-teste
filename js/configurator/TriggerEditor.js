// TriggerEditor.js — sistema de narrativa (a implementar)
// Cria áreas invisíveis (Box3) no viewport para project.triggers, desenhadas
// como wireframe semi-transparente (igual ao cone de visão do GuardEditor)
// para você posicionar/redimensionar visualmente, e configura
// triggerEvent/action/payload por trigger.
export class TriggerEditor {
  constructor({ container, sceneManager, projectManager }) {
    this.container = container;
    this.sceneManager = sceneManager;
    this.projectManager = projectManager;
  }
  render() {
    // TODO
  }
}
