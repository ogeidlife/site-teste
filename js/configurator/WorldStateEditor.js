// WorldStateEditor.js — sistema de narrativa (a implementar)
// Painel "WORLD STATE" com botões [BEFORE] [AFTER] [STATE 03] [STATE 04] por
// objeto, e o botão "PREVIEW TRANSFORMATION" que alterna visualmente o objeto
// selecionado entre seus estados (troca o assetId exibido) sem alterar o
// project.worldState real — é só pré-visualização de edição.
export class WorldStateEditor {
  constructor({ container, sceneManager, projectManager }) {
    this.container = container;
    this.sceneManager = sceneManager;
    this.projectManager = projectManager;
  }
  render() {
    // TODO
  }
}
