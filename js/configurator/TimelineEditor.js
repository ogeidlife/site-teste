// TimelineEditor.js — editor visual do TimelineSystem.js (opcional, a implementar).
export class TimelineEditor {
  constructor({ container, projectManager }) {
    this.container = container;
    this.projectManager = projectManager;
  }
  render() {
    // TODO
  }
}
