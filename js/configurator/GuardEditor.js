// GuardEditor.js — FASE 3 (a implementar)
// Cria/edita project.guards. No viewport: clicar para adicionar pontos de
// waypoint (PONTO A -> B -> C -> D) desenhados como uma polyline, e o cone de
// visão do guarda (visionDistance/visionAngle) desenhado como um
// THREE.Mesh semi-transparente que acompanha a rotação do guarda selecionado.
export class GuardEditor {
  constructor({ container, sceneManager, projectManager }) {
    this.container = container;
    this.sceneManager = sceneManager;
    this.projectManager = projectManager;
  }
  render() {
    // TODO Fase 3
  }
}
