// MissionEditor.js — FASE 6 (a implementar)
// Painel de configurador para criar/editar project.missions sem programar:
// nome da missão, lista ordenável de objetivos (GO_TO, COLLECT, PAINT, ESCAPE,
// RETURN, INTERACT, SURVIVE, AVOID_DETECTION), cada um com id/nome/descrição/
// posição (escolhida clicando na viewport, reaproveitando SceneManager.pickAt
// ou um modo "place point")/condição/recompensa, e o campo videoIntro
// (arquitetura de narrativa já reservada em project.videos).
export class MissionEditor {
  constructor({ container, projectManager }) {
    this.container = container;
    this.projectManager = projectManager;
  }
  render() {
    // TODO Fase 6
  }
}
