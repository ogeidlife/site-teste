// CameraEditor.js — FASE 4 (a implementar)
// Cria/edita project.securityCameras (posição/rotação/fov/detectionDistance/
// rotationSpeed/patrolAngle/mode FIXED|ROTATING) — cone de visão desenhado
// no viewport igual ao GuardEditor. Também edita project.camera (câmera
// principal do jogo: isométrica/topdown/terceira pessoa), que hoje só existe
// no JSON com valores padrão (ver ProjectManager.js).
export class CameraEditor {
  constructor({ container, sceneManager, projectManager }) {
    this.container = container;
    this.sceneManager = sceneManager;
    this.projectManager = projectManager;
  }
  render() {
    // TODO Fase 4
  }
}
