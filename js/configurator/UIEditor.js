// UIEditor.js — FASE 8 (a implementar)
// Edita project.ui: checkboxes HP/Energy/XP/Mission Panel/Minimap/Inventory/
// Camera Feed + posição/tamanho/transparência/escala de cada elemento
// (gravado em project.ui.layout). O game/UIManager.js lê esse layout em vez
// de ter qualquer elemento de HUD hardcoded, como pedido no spec (item 16).
export class UIEditor {
  constructor({ container, projectManager }) {
    this.container = container;
    this.projectManager = projectManager;
  }
  render() {
    // TODO Fase 8
  }
}
