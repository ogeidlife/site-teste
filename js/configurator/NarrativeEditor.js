// NarrativeEditor.js — "NARRATIVE SEQUENCE EDITOR" (a implementar).
// Monta visualmente a cadeia [VIDEO] -> [OBJECTIVE] -> [TRIGGER] -> [VIDEO] ->
// [ACTION] -> [GAMEPLAY] -> [STATE CHANGE] -> [VIDEO] -> [REWARD] por missão,
// gerando/editando a sequência que o EventSystem.js roda em Play Mode.
export class NarrativeEditor {
  constructor({ container, projectManager }) {
    this.container = container;
    this.projectManager = projectManager;
  }
  render(missionId) {
    // TODO
  }
}
